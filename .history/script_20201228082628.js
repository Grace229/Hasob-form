const genger = document.querySelector("input[name='gender']");
const form = document.querySelector("form");

function submitForm(e) {
  e.preventDefault();
  console.log(gender);
}

form.addEventListener('submit', submitFormnt))

